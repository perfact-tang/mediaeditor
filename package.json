{
  "name": "mediasplitter-ai-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 MVP for MediaSplitter AI: import, preview, manual segments, and FFmpeg stream-copy splitting.",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "tauri": "tauri",
    "prepare:ffmpeg": "node scripts/prepare-ffmpeg.js",
    "tauri:dev": "npm run prepare:ffmpeg && tauri dev",
    "tauri:build": "npm run prepare:ffmpeg && tauri build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "ffmpeg-static": "^5.3.0"
  }
}
